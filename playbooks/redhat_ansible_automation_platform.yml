---

- name: Red Hat Ansible Automation Platform (AAP)
  hosts: "{{ target_hosts | default('aap_installer') }}"
  gather_facts: false
  become: true
  become_user: root
  any_errors_fatal: true
  tags:
    - redhat
    - aap
  roles:
    - infra.soe.redhat_ansible_automation_platform
