---

- name: scap | Load SCAP profile specific variables
  ansible.builtin.include_vars: "{{ __redhat_image_builder_scap_vars_file }}"
  vars:
    __redhat_image_builder_scap_vars_file: "{{ role_path }}/vars/scap/{{ redhat_image_builder_scap_profile | regex_replace('xccdf_org.*.content_profile_', '') }}"
  when:
    - redhat_image_builder_scap_profile is defined
    - __redhat_image_builder_scap_vars_file is file

- name: scap | Security Content Automation Protocol (SCAP) Remediation
  ansible.builtin.include_role:
    name: infra.soe.scap
    apply:
      tags: image_builder_scap
  vars:
    scap_tailoring_title: "{{ redhat_image_builder_scap_tailoring_title }}"
    scap_tailoring_description: "{{ redhat_image_builder_scap_tailoring_description }}"
    scap_tailoring_exceptions: "{{ redhat_image_builder_scap_tailoring_title }}"
  tags: image_builder_scap
  when: redhat_image_builder_scap_profile is defined
